import type { Meta } from '@storybook/react';

import ToolList from './ToolList';

export default {
  title: 'Pages/ToolList',
  component: ToolList,
} as Meta;

export const Default = () => <ToolList />;
